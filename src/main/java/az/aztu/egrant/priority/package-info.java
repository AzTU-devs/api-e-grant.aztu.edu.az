/**
 * Research-priority lookup (was the legacy {@code prioritets}/{@code priotet} table): the
 * thematic categories a project is tagged with. Exposes a cross-module {@code PriorityDirectory}.
 */
@org.springframework.modulith.ApplicationModule(
        displayName = "Priority")
package az.aztu.egrant.priority;
