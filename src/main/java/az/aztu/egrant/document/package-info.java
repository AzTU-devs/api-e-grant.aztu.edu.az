/**
 * Document generation: project + smeta exports as PDF (openhtmltopdf) and Excel (Apache POI).
 * A read-only aggregator that gathers data exclusively through other modules' published APIs
 * ({@code project}, {@code budget}, {@code iam}, {@code institution}, {@code priority}).
 */
@org.springframework.modulith.ApplicationModule(
        displayName = "Document")
package az.aztu.egrant.document;
