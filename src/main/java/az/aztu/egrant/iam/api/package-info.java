/**
 * Published API of the {@code iam} module: cross-module lookups ({@link az.aztu.egrant.iam.api.UserDirectory})
 * and domain events. Exposed as a Spring Modulith named interface so other modules may depend on it.
 */
@org.springframework.modulith.NamedInterface("api")
package az.aztu.egrant.iam.api;
