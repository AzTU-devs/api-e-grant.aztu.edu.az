/**
 * Published API of the {@code budget} module: the {@link az.aztu.egrant.budget.api.BudgetDirectory}
 * read access used by {@code document} to render the smeta (totals, salary and cost lines).
 */
@org.springframework.modulith.NamedInterface("api")
package az.aztu.egrant.budget.api;
